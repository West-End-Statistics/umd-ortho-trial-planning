% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{simulate_data}
\alias{simulate_data}
\title{Simulate Clinical Trial Data Using NORTA Methodology}
\usage{
simulate_data(
  n,
  arm = c("placebo", "active"),
  outcome_correlation = 0,
  survival_prob = c(0.85, 0.9),
  days_at_home_mean = c(67, 78),
  days_at_home_sd = c(20, 20),
  placebo_amb_status = c(0.07, 0.37, 0.34),
  amb_status_latent_shift = 0.2,
  survival_missing = 0,
  days_at_home_missing = 0,
  amb_status_missing = 0
)
}
\arguments{
\item{n}{Integer. Number of subjects to simulate.}

\item{arm}{Character. Trial arm, either "placebo" or "active".}

\item{outcome_correlation}{Numeric. Correlation coefficient between outcomes (default: 0).}

\item{survival_prob}{Numeric vector of length 2. Survival probabilities for
placebo and active arms (default: c(0.85, 0.90)).}

\item{days_at_home_mean}{Numeric vector of length 2. Mean days at home for
placebo and active arms (default: c(67, 78)).}

\item{days_at_home_sd}{Numeric vector of length 2. Standard deviation of days
at home for both arms (default: c(20, 20)).}

\item{placebo_amb_status}{Numeric vector of length 3. Ambulatory status proportions
for placebo arm (default: c(0.07, 0.37, 0.34)).}

\item{amb_status_latent_shift}{Numeric. Latent shift for ambulatory status in
active arm (default: 0.2).}

\item{survival_missing}{Numeric. Proportion of missing survival data (default: 0).}

\item{days_at_home_missing}{Numeric. Proportion of missing days at home data (default: 0).}

\item{amb_status_missing}{Numeric. Proportion of missing ambulatory status data (default: 0).}
}
\value{
A data frame with columns:
\item{died}{Binary indicator of death (0 = survived, 1 = died)}
\item{days_at_home}{Integer days at home (1-121)}
\item{ambulation_status}{Ordered factor of ambulatory status}
\item{amb_status_numeric}{Numeric version of ambulatory status}
}
\description{
Generates synthetic patient data for a single trial arm using the NORTA
(Normal-to-Anything) correlation structure. Simulates hierarchical outcomes
including survival, days at home, and ambulatory status with configurable
correlation and missingness.
}
\examples{
# Simulate 100 placebo patients
placebo_data <- simulate_data(n = 100, arm = "placebo")

# Simulate 100 active arm patients with correlation
active_data <- simulate_data(n = 100, arm = "active", outcome_correlation = 0.3)
}
